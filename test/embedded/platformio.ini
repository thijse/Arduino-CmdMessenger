; PlatformIO native test environment for CmdMessenger
; Runs on the host (no hardware needed) using a Stream shim.

[env:native]
platform = native
build_flags =
    -std=c++11
    -DARDUINO=100
lib_extra_dirs =
    lib
test_framework = unity
