; Loopback firmware built as a native executable.
; Communicates via stdin/stdout pipes for integration testing.

[env:native]
platform = native
build_flags =
    -std=c++11
    -DARDUINO=100
lib_extra_dirs =
    ../../embedded/lib
